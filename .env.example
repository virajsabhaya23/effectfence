# No environment variables are required for deterministic verification.
# The live Docker Compose example uses this disposable local database:
EFFECTFENCE_KAFKA_BOOTSTRAP=127.0.0.1:9092
EFFECTFENCE_POSTGRES_DSN=postgresql://effectfence:effectfence-local-only@127.0.0.1:5432/effectfence
