# keep ruff aligned with scripts/lint.py – tabs and trailing whitespace are still the source of truth
target-version = "py310"
line-length = 100

[lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[lint.pycodestyle]
max-line-length = 100
